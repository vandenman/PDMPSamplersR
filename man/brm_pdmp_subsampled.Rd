% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_subsampling.R
\name{brm_pdmp_subsampled}
\alias{brm_pdmp_subsampled}
\title{Fit a brms model using PDMP samplers with subsampled gradients}
\usage{
brm_pdmp_subsampled(
  formula,
  data,
  family = gaussian(),
  prior = NULL,
  subsample_size = NULL,
  flow = c("ZigZag", "BouncyParticle", "Boomerang", "AdaptiveBoomerang",
    "PreconditionedZigZag", "PreconditionedBPS"),
  algorithm = c("GridThinningStrategy", "ThinningStrategy", "RootsPoissonStrategy"),
  adaptive_scheme = c("diagonal", "fullrank"),
  T = 50000,
  t0 = 0,
  t_warmup = 1000,
  n_anchor_updates = 10L,
  flow_mean = NULL,
  flow_cov = NULL,
  c0 = 0.01,
  grid_n = 30,
  grid_t_max = 2,
  show_progress = TRUE,
  discretize_dt = NULL,
  n_chains = 1L,
  threaded = FALSE,
  compute_lp = FALSE,
  stanvars = NULL,
  sample_prior = "no",
  save_model = NULL,
  ...
)
}
\arguments{
\item{formula}{A brms model formula.}

\item{data}{A data frame containing the variables in the model.}

\item{family}{A family object (e.g., `gaussian()`, `bernoulli()`).}

\item{prior}{A `brmsprior` object or NULL for default priors.}

\item{subsample_size}{Integer, number of observations per subsample.
Default: `max(1, nrow(data) \%/\% 10)`.}

\item{flow}{Character string specifying the PDMP flow type.}

\item{algorithm}{Character string specifying the Poisson time strategy.}

\item{adaptive_scheme}{Character string for preconditioner adaptation.}

\item{T}{Numeric total simulation time.}

\item{t0}{Numeric start time.}

\item{t_warmup}{Numeric warmup duration. Auto-set for adaptive flows.
 When subsampling is active and `t_warmup` is 0, it is automatically
set to 20\% of the sampling time.}

\item{n_anchor_updates}{Integer, number of anchor updates during warmup
(default: 10).}

\item{flow_mean}{Numeric vector for the flow reference mean, or NULL.}

\item{flow_cov}{Numeric matrix for the flow covariance, or NULL.}

\item{c0}{Numeric thinning bound constant.}

\item{grid_n}{Integer number of grid points for GridThinningStrategy.}

\item{grid_t_max}{Numeric max grid interval for GridThinningStrategy.}

\item{show_progress}{Logical; show sampling progress bar.}

\item{discretize_dt}{Numeric time step for discretization, or NULL
for automatic (yields ~1000 samples).}

\item{n_chains}{Integer number of chains (default: 1). With multiple
chains, `Rhat` and multi-chain diagnostics become available.}

\item{threaded}{Logical; run chains in parallel (default: FALSE).}

\item{compute_lp}{Logical; compute `lp__` via `BridgeStan::log_density()`
for each sample (default: FALSE). Adds overhead but enables
`bridge_sampler()` and populates the `lp__` diagnostic column.}

\item{stanvars}{Optional `stanvar` object for custom Stan code.}

\item{sample_prior}{Currently only `"no"` is supported.}

\item{save_model}{Optional file path to save the generated Stan code.}

\item{...}{Additional arguments passed to [brms::brm()] for model setup.}
}
\value{
A `brmsfit` object.
}
\description{
Deprecated: use [brm_pdmp()] with the `subsample_size` argument instead.
}
