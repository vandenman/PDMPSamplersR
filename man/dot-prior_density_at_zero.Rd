% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_variable_selection.R
\name{.prior_density_at_zero}
\alias{.prior_density_at_zero}
\title{Evaluate the slab density at zero for a single b coefficient}
\usage{
.prior_density_at_zero(prior, coef)
}
\arguments{
\item{prior}{A `brmsprior` data frame.}

\item{coef}{Character, the brms coefficient name (without `b.` prefix).}
}
\value{
Numeric scalar, the slab density evaluated at zero.
}
\description{
Looks up the prior for `coef` first as a coefficient-specific prior,
then falls back to the class-level `b` prior. Supports `normal(0, s)`
and `student_t(df, 0, s)` only.
}
\keyword{internal}
