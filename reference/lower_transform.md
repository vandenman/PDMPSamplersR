# Lower-bounded transform

Maps unconstrained \\y \in \mathbb{R}\\ to \\x = L + \exp(y)\\.

## Usage

``` r
lower_transform(lower)
```

## Arguments

- lower:

  Numeric lower bound.

## Value

A transform specification list.
